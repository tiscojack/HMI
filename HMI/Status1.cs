namespace Prova
{
    // Enum that represents the possible different states of a system.
    public enum Status1
    {
        FAILURE,
        DEGRADED,
        MAINTENANCE,
        UNKNOWN,
        OPERATIVE, 
        NOSTATUS
    }
}