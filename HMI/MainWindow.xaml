<Window
    x:Class="Prova.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Prova"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    SizeToContent="Manual"
    WindowState="Maximized"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="150" MaxWidth="800"/>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
            <DockPanel x:Name="leftDocPanel">
            <ScrollViewer>
                <StackPanel>
                <TextBox x:Name="txtSearch" ToolTip="Search Here" VerticalAlignment="Top" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" MinWidth="200" TextChanged="txtSearch_TextChanged" Grid.Column="0" Grid.Row="1"/>
                <TreeView
                    Name="dirTree"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    MouseUp="tv_MouseUp"
                    VirtualizingStackPanel.IsVirtualizing="False"
                    VirtualizingStackPanel.VirtualizationMode="Standard">


                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded" Value="True" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            
            </StackPanel>
            </ScrollViewer>
        </DockPanel>
            <GridSplitter
                Grid.Column="1"
                Width="5"
                HorizontalAlignment="Stretch" />
            <DockPanel x:Name="rightDocPanel" Grid.Column="2">
            <ScrollViewer>
                <DockPanel
                    x:Name="DocPanel"
                    Grid.Column="2"
                    Background="#00A3CC"
                    >
                    <WrapPanel
                        x:Name="Wrap"
                        Grid.Column="2"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        ItemWidth="170" />
                </DockPanel>
            </ScrollViewer>
        </DockPanel>

    </Grid>
</Window>
